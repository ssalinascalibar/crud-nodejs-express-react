{
    "version": 2,
    "builds": [
        {
            "src": "*.js",
            "use": "@vercel/node",
            "config": {
                "includeFiles": ["users.json"]
              }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}